# Input data

Synthetic teaching data. `pozos.csv`, `mediciones.csv`, and `datos_hidrogeologicos.xlsx` contain the same two logical tables. Controlled issues in `mediciones`: one missing head-depth value, one negative precipitation value, one unusually high conductivity value, and one exact duplicate.
